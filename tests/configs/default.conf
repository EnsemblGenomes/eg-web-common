# DEFAULT CONFIGS
# This file provides a set of defaults.
# You can override these settings in your config file or from the command line.

{
  #----------------------------------------------------------------------------
  # SPECIES
  # required for species-specific tests
  #----------------------------------------------------------------------------

  species => undef,  # the species to test e.g. Zea_mays

  #----------------------------------------------------------------------------
  # SELENIUM 
  # required only for selenium based tests
  #----------------------------------------------------------------------------

  selenium_host     => '172.22.68.98',
  selenium_port     => 4444,
  selenium_browser  => 'firefox',
  selenium_timeout  => 30000,

  #----------------------------------------------------------------------------
  # LIVE
  # required only for live tests 
  #----------------------------------------------------------------------------
  
  live_url          => undef, # the live url for the site e.g. http://plants.ensembl.org   
  live_data_db_host => undef, 
  live_user_db_host => undef, 
}
