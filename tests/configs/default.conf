# DEFALT CONFIGS
# This file provides a base - you can (and must) override 
# the values in your config file or from the command line

{
  # GENERAL 

  species           => undef,  # REQUIRED - the species to test e.g. Zea_mays
  division          => undef,  # REQUIRED - the site division e.g. plants

  # SELENIUM 
  # OPTIONAL - needed only for selenium based tests

  selenium_host     => '172.22.68.98',
  selenium_port     => 4444,
  selenium_browser  => 'firefox',
  selenium_timeout  => 30000,

  # LIVE
  # OPTIONAL - needed only for live environment tests 

  live_url          => undef, # the live url for the site e.g. http://plants.ensembl.org   
  live_data_db_host => undef, # the hostname for the live db
  live_user_db_host => undef, # the hostname for live user db
}
