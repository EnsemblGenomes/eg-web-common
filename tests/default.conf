# Default settings - can override with command line args

{
  url              => '<URL to test>',
  species          => '<Species to test, e.g. Zea_mays>',

  selenium_host    => '<IP of selenium server host>',
  selenium_port    => 4444,
  selenium_browser => 'firefox',
  selenium_timeout => 30000,
}